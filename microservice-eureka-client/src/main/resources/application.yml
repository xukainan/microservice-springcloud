server:
  port: 8000
eureka:
  instance:
    prefer-ip-address: true  #是否显示主机ip
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/   #制定eureka服务端地址
spring:
  application:
    name: microservice-eureka-user  #指定应用名称
